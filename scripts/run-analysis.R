# ============================================================================
# RUN ANALYSIS  —  runs the project's main script and shows the results
# ============================================================================
# This runs analysis.R from top to bottom and prints its output to the Console.
# It also writes a chart to the "output" folder.
#
# HOW TO RUN IT:
#   In RStudio, open this file and click "Source" (or press Cmd+Shift+S).
#
# (You can also open analysis.R directly and Source that — this file is just a
#  convenient, clearly-labelled shortcut.)

library(here)
source(here("analysis.R"))
